# --- Python ---
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# --- OS / editor cruft ---
.DS_Store
*.swp
.idea/
.vscode/

# --- Customer data / DART bundles (NEVER commit) ---
*.zip
*.tar.gz
*.tgz
*.evtx
*.pcap
*.pcapng
DARTLogs/
Logs/
# Working folder for UZTNA evidence - holds extracted DART bundles.
uZTNA-Component/
docs/deck-img/
# Built from docs/deck-img, so it embeds the same customer screenshots.
ATLAS-Overview.pptx
uploads/
instance/

# --- Credentials (NEVER commit) ---
github
github.pub
*.pem
id_rsa*
id_ed25519*

# --- Temp / scratch files ---
.tmp_*.js
*.tmp
feedback.rtf

# --- Stray assets ---
kisspng-*.webp

# Evidence and capture material - NEVER COMMIT.
# A HAR carries full HTTP traffic including headers and cookies; a capture plus
# its key log decrypts the session it recorded; a DART bundle carries endpoint
# and organisation identifiers.
*.har
*.cap
*.keys
*.keylog
sslkeys*.log
keylog*.txt
TEST DATA DO NOT UPLOAD/
